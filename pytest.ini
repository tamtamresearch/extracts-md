[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    requires_libreoffice: mark test as requiring LibreOffice (soffice)
    integration: mark test as implementation-agnostic integration test
    unit: mark test as implementation-specific unit test
